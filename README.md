# نظام إدارة التذاكر 🎫
## Ticket Management System

نظام متكامل لإدارة ومتابعة تذاكر العمل مع دعم كامل للغة العربية والإنجليزية.

---

## 📁 هيكل المشروع

```
ticket-system/
├── index.html              # الصفحة الرئيسية
├── css/
│   └── styles.css          # التنسيقات المخصصة
├── js/
│   ├── config.js           # إعدادات Supabase والتطبيق
│   ├── utils.js            # الدوال المساعدة
│   ├── api.js              # عمليات قاعدة البيانات
│   ├── components.js       # مكونات React UI
│   └── app.js              # المنطق الرئيسي للتطبيق
├── assets/                 # الصور والأيقونات
└── README.md               # هذا الملف
```

---

## ⚙️ الإعدادات

### 1. Supabase
عدّل ملف `js/config.js`:
```javascript
const CONFIG = {
    SUPABASE_URL: 'https://your-project.supabase.co',
    SUPABASE_ANON_KEY: 'your-anon-key',
    // ...
};
```

### 2. التصنيفات (Tags)
أضف تصنيفات جديدة في `js/config.js`:
```javascript
TAGS: [
    { id: 'faroqat', name: 'فروقات', color: 'bg-amber-500' },
    { id: 'urgent', name: 'عاجل', color: 'bg-red-500' },
    { id: 'review', name: 'مراجعة', color: 'bg-purple-500' },
]
```

---

## 🗄️ قاعدة البيانات (Supabase)

### جدول tickets
```sql
CREATE TABLE tickets (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    ticket_number INTEGER NOT NULL,
    name TEXT NOT NULL,
    link TEXT,
    details TEXT,
    attachments JSONB DEFAULT '[]',
    status TEXT DEFAULT 'pending',
    date DATE DEFAULT CURRENT_DATE,
    tags TEXT[] DEFAULT '{}',
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Storage Bucket
اسم: `attachments`
- Public access: ✅
- مسار الرفع: `uploads/`

---

## 🚀 التشغيل

### الطريقة 1: Server محلي (مُوصى به)
```bash
# باستخدام Python
python -m http.server 8000

# أو باستخدام Node.js
npx serve .

# أو باستخدام PHP
php -S localhost:8000
```
ثم افتح: `http://localhost:8000`

### الطريقة 2: مباشرة
استخدم الملف المدمج `../index.html` في المجلد الأب.

---

## 📝 الملفات

### config.js
- إعدادات Supabase (URL, Key)
- قائمة التصنيفات
- قائمة الحالات
- إعدادات الملفات

### utils.js
- `getFileIcon()` - أيقونة الملف حسب النوع
- `isImage()` - التحقق من نوع الصورة
- `detectDirection()` - اكتشاف اتجاه النص (RTL/LTR)
- `parseTelegramMessage()` - تحليل رسائل Telegram

### api.js
- `fetchTickets()` - جلب التذاكر
- `createTicket()` - إنشاء تذكرة
- `updateTicket()` - تحديث تذكرة
- `deleteTicket()` - حذف تذكرة
- `uploadFile()` - رفع ملف
- `deleteFile()` - حذف ملف

### components.js
- `TicketForm` - نموذج التذكرة
- `TicketCard` - بطاقة التذكرة
- `Statistics` - الإحصائيات
- `SearchFilter` - البحث والفلترة
- `Modal` - النوافذ المنبثقة
- `Lightbox` - عرض الصور

### app.js
- المنطق الرئيسي للتطبيق
- إدارة الحالة (State)
- التعامل مع الأحداث

---

## ✨ المميزات

- ✅ إضافة وتعديل وحذف التذاكر
- ✅ رفع المرفقات (صور، ملفات)
- ✅ نظام التصنيفات
- ✅ البحث والفلترة
- ✅ استيراد من Telegram
- ✅ تصدير واستيراد JSON
- ✅ دعم RTL/LTR تلقائي
- ✅ تصميم متجاوب

---

## 🔮 التطوير المستقبلي

- [ ] نظام المستخدمين والصلاحيات
- [ ] لوحة تحكم بالرسوم البيانية
- [ ] نظام الإشعارات
- [ ] التعليقات على التذاكر
- [ ] سجل التغييرات
- [ ] تطبيق PWA
- [ ] API خارجي

---

## 📄 الترخيص

MIT License
